spring:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-test.yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:5432}/animalshelter
    username: test
    password: test